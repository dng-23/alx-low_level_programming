#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers.
 * @a: the first number to be multiplied
 * @b: the second number to be multiplied
 * Return: the multiple
 */
int mul(int a, int b)
{
	return (a * b);
}
