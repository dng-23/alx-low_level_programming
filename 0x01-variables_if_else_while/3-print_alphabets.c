#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char cl = 'a', cu = 'A';

	while (cl <= 'z')
	{
		putchar (ch);
		ch++;
	}

	while (cu <= 'Z')
	{
		putchar (ch);
		ch++;
	}

	putchar ('\n');

	return (0);
}
